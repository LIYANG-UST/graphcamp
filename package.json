{
  "name": "camp",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.thegraph.com/deploy/ liyang-ust/camp",
    "create-local": "graph create --node http://localhost:8020/ liyang-ust/camp",
    "remove-local": "graph remove --node http://localhost:8020/ liyang-ust/camp",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 liyang-ust/camp",
    "test": "graph test"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.58.0",
    "@graphprotocol/graph-ts": "0.30.0"
  },
  "devDependencies": { "matchstick-as": "0.5.0" }
}
